#include <iostream>
#include <string>
//#include "C:\Users\X220\Documents\Visual Studio 2015\Projects\Prog2a\PA08\Bruch.h"
#include "C:\Users\X220\Documents\Visual Studio 2015\Projects\Prog2a\blatt26aufg1a"

/*
string leseArtikel(string const& dateiName)
{
	string rohartikel = "";
	string zeile = "";
	//einlesen in zwischenvariable namens zeile
	ifstream inputdatei(dateiName.c_str());
	getline(cin, rohartikel);

	if (!inputDatei)
		return rohartikel;

	while (getline(dateiName, rohartikel))
	{




	}
*/
int main()
{
	/*cout << "gib wörter ein: " << endl;
	
	string inputName;
	getline(cin, inputName);
	ofstream outputdatei("neueDatei2" + inputName);
	
	if (!outputdatei)
		return 

	string neueDatei = ""

*/
	Bruch bruch(2,3);
	Bruch bruch2(4, 8);
	cout <<bruch / bruch2<< endl;

	// sortiere Vektoren lexikografisch

	Vektor2d 
	for (int i = 0; i < NVEK; ++i)	// durchgehen von einträge der "argumenten"
		for (int j = i + 1; j < NVEK; ++j)	//durchgehen von einträge von "Objekte" (=auch argumente)
/*
			if (1 == vek[i].vergleiche(vek[j]))
				vek[i].vertausche(vek[j]);
	// wenn 1 bei einer komponente festgestellt wurde ==> dann komplettes paar wird an diese stelle kopiert
	cout << "ausgabe vek[0]" << vek[0] << endl;
	cout << "ausgabe vek[1]" << vek[1] << endl;
	//cout << "ausgabe vek[2]" << vek[2] << endl;
	//cout << "ausgabe vek[3]" << vek[3] << endl;
*/
	return 0;
}