/*
int div(int m, int n)
{
	if ((m >= 0) == (n >= 0)
		|| m % n == 0)
		return m / n;
	else
		return m / n - 1;
}
*/

#include <iostream>
using namespace std;




int main()
{
	/*
	int n1, n2;
	int ergebnis;
	cout << "gib eine zahl n1 ein : " << endl;
	cin >> n1;
	cout << "gib eine zahl n2 ein : " << endl;
	cin >> n2;

	ergebnis = (n1 % n2);

	cout << "ergebnis der division n1 / n2 : " << ergebnis << endl;

	
	int aufgerundet, n, m;
	
	cout << "gib n ein: " << endl;
	cin >> n;
	cout << "gib m ein: " << endl;
	cin >> m;
	aufgerundet = div(n, m);
	cout << "das aufgerundete ergebnis lautet: " << aufgerundet << endl;
	*/

	return 0;
}

